# App icons

The web manifest references `icon-192.png` and `icon-512.png`. Add real
PNG assets here during the design phase (earth-tone + money-green direction,
SOW §3.1). Until then, "Add to Home Screen" falls back to a screenshot icon.
